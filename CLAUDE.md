# 작업 규칙

## PR 생성

이 레포지토리는 **GitHub Pages를 통해 확인**하기 때문에, 변경 사항이
main에 머지되어야 사용자가 결과를 볼 수 있다.

따라서 어떤 변경이든 다음 흐름을 따른다:

1. 작업 브랜치(`claude/...`)에서 작업
2. commit & push
3. **반드시 PR을 생성**해서 사용자에게 머지할 수 있게 한다
   (별도 요청이 없어도 자동으로)
4. 같은 브랜치에 이어 작업하는 동안 PR이 이미 열려 있으면 새로 만들지 않고
   기존 PR에 push만으로 반영. 사용자가 머지하면 그 다음 변경 사항부터는
   새 PR을 만든다.

PR 제목·본문은 한국어로, 요약·문제·수정·Test plan 형식으로 정리.
